# Render Blueprint — deploy API + static frontend from one repo.
# Dashboard → New → Blueprint → connect this repository.
# Set secret env vars (DB_*, CORS_ORIGIN, VITE_API_URL) when prompted.

services:
  - type: web
    name: l2essencepowerprism-api
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    plan: free
    envVars:
      - key: NODE_VERSION
        value: "20"
      - key: DB_HOST
        sync: false
      - key: DB_PORT
        value: "3306"
      - key: DB_USER
        sync: false
      - key: DB_PASSWORD
        sync: false
      - key: DB_NAME
        sync: false
      - key: CORS_ORIGIN
        sync: false
      - key: XP_RATE
        value: "40"
      - key: SP_RATE
        value: "10"
      - key: ADENA_RATE
        value: "10"
      - key: DROP_RATE
        value: "1"
      - key: DUALBOX
        value: disabled
      - key: VITE_DISCORD_URL
        sync: false
      - key: VITE_DOWNLOAD_URL
        sync: false
      - key: VITE_API_URL
        sync: false

  - type: web
    name: l2essencepowerprism
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
